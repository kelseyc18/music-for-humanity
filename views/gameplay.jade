extends layout

block content
  h1= title

  if error
    p Error getting dynamic content.
  else
    p Join one of the following !{data.game_count} game(s):

    ul
    each game in data.game_list
      li
        | #[b #{game.name}] (#[em #{game._id}]) - 
        a(href=game.url) View players

    else
      li There are no games.

  br
  button(type='button' class='btn btn-primary' disabled) Create new game
